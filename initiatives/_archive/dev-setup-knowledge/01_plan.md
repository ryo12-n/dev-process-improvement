# 実施計画: 開発環境セットアップ手順の Knowledge 管理

## フェーズ構成

| フェーズ | 内容 | 期間 | 成功基準 |
|---------|------|------|----------|
| 1 | 調査・ドキュメント作成・スクリプト作成 | 1セッション | `docs/dev-setup.md` と `scripts/setup.sh` が作成され、README にリンクが追加されている |
| 2 | 動作検証・レビュー（ゲート判定後） | 1セッション | `setup.sh` を実行して手順書の内容と一致することを確認 |

## スケジュール

- **Day 1（フェーズ1）**: L2-worker がツール調査・ドキュメント作成・スクリプト作成を実施
- **ゲート判定**: L1 が成果物レビューの上 `08_gate_review.md` に判定を記録
- **Day 2（フェーズ2）**: L2-evaluator が動作検証を実施し、`06_eval_report.md` を提出

## 成功基準（全体）

1. `docs/dev-setup.md` に必要ツール一覧・バージョン要件・インストール手順が記載されている
2. `scripts/setup.sh` が Linux/macOS 上で実行でき、不足ツールを検出して案内できる
3. `README.md` に `docs/dev-setup.md` へのリンクが追加されている
4. 既存の `docs/` や `scripts/` に意図しない変更が生じていない

## リソース・前提条件

- 対象リポジトリ: `/home/user/ai-driven-dev-patterns`
- L2-worker が参照すべき既存ファイル:
  - `dev-process-improvement/refs/ai-driven-development-poc/scripts/init_project.sh`（参考スクリプト）
  - `.claude/settings.json`（許可コマンド確認用）
  - `README.md`（リンク追加先）
- ツールの確認は `which` / `--version` コマンドで実施する

## リスクと対策

| リスク | 発生確率 | 影響 | 対策 |
|--------|---------|------|------|
| 必要ツールの調査漏れ | 中 | 中 | 既存スクリプト・コマンドの grep 調査を義務付ける |
| `setup.sh` がインストールを強制実行する副作用 | 低 | 中 | check-only モード（ツールの有無確認のみ）を基本動作とする |
| ドキュメントの陳腐化 | 中 | 低 | `docs/dev-setup.md` の末尾に更新履歴セクションを設ける |

---
**作成者**: L1
**作成日**: 2026-02-23
**最終更新**: 2026-02-23
