# サブエージェントの権限許可モデル調査

## 気づき

- L2サブエージェント（Agent ツール）内で実行される Bash コマンドが、`.claude/settings.json` の allow リストで許可済みにも関わらずユーザーに許可を求めていた可能性がある
- サブエージェント専用の allow リストは存在しない認識
- マネージャー（L1）がサブエージェントの許可を代理で行う仕組みもない認識

## 調査したいこと

- サブエージェント（Agent ツール）は `.claude/settings.json` の allow リストを継承するのか？
- 継承しない場合、サブエージェント向けの権限設定方法はあるか？
- Claude Code の公式ドキュメントでの仕様確認

## 発生契機

- `session-branch-cleanup-flow` 施策のL2サブエージェント実行時に、allow リスト済みの操作で許可プロンプトが出ていた（可能性）
