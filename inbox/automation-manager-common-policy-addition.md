# manager-common-policy §1 に automation-manager を追加

## 概要

`manager-common-policy §1` の適用対象テーブルに `automation-manager` を追加する。

## 背景

施策 `automation-manager-session` で `.claude/skills/automation-manager/` を新規作成したが、共通ポリシー側の適用対象テーブルへの追記は施策スコープ外としていた。

## 出典

- `sessions/initiatives/automation-manager-session/08_gate_review.md` 必須把握事項 #1

## 期待するアクション

- `.claude/skills/manager-common-policy/SKILL.md` §1 の適用対象テーブルに `automation-manager` 行を追加
