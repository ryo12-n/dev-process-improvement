# AI-Driven Development Framework

並列Claudeチーム運用のための汎用フレームワーク/テンプレートです。複数のClaudeエージェントが協調して、タスクを自律的に実行します。

## 概要

このフレームワークは、Anthropic社の記事「[Building a C Compiler](https://www.anthropic.com/engineering/building-c-compiler)」で紹介された並列Claude活用手法を汎用化したものです。

### 主な特徴

- **並列作業**: 複数のClaudeエージェントが同時に異なるタスクを実行
- **自動競合回避**: テキストファイルベースのロック機構で競合を防止
- **役割分担**: 7種類の専門ロール（機能実装、バグ修正、リファクタリングなど）
- **進捗自動記録**: Git フックによる進捗の自動記録
- **視認性**: tmuxによるリアルタイム監視

## クイックスタート

### 1. 初期セットアップ

```bash
# ディレクトリ構造とスクリプトの生成
./scripts/init_project.sh

# Git フックのインストール
./scripts/setup_hooks.sh
```

### 2. プロジェクトのカスタマイズ

```bash
# テストスクリプトを実装
vi ci/run_fast.sh    # 高速チェック（30秒以内）
vi ci/run_full.sh    # フルテスト（5分以内）

# タスクを追加
cp tasks/TASK_TEMPLATE.md tasks/features/001_my_feature.md
vi tasks/features/001_my_feature.md
```

### 3. エージェントチームの起動

```bash
# チーム構成の確認（dry-run）
./scripts/launch_team.sh --dry-run

# チームを起動
./scripts/launch_team.sh
```

### 4. 進捗の確認

```bash
# 稼働状況の確認
./scripts/status.sh

# または、tmuxセッションで直接確認
tmux attach -t agents
```

## ディレクトリ構成

```
.
├── .claude/                    # Claude エージェント設定
│   ├── base_prompt.md          # 全エージェント共通の指示
│   ├── roles/                  # ロール別プロンプト（7種類）
│   ├── protocols/              # プロトコル定義（3種類）
│   └── hooks/                  # Git フック
│
├── tasks/                      # タスクキュー
│   ├── features/               # 機能実装タスク
│   ├── bugs/                   # バグ修正タスク
│   ├── refactor/               # リファクタリングタスク
│   ├── tests/                  # テスト追加タスク
│   ├── optimize/               # 最適化タスク
│   └── docs/                   # ドキュメントタスク
│
├── locks/                      # タスクロック（排他制御）
├── logs/                       # エージェント活動ログ
├── done/                       # 完了タスクのアーカイブ
│
├── ci/                         # CI/テストスクリプト
│   ├── run_fast.sh             # 高速チェック
│   └── run_full.sh             # フルテスト
│
├── scripts/                    # 管理スクリプト
│   ├── init_project.sh         # 初期セットアップ
│   ├── launch_agent.sh         # 単一エージェント起動
│   ├── launch_team.sh          # チーム起動（tmux）
│   ├── status.sh               # 稼働状況確認
│   ├── setup_hooks.sh          # フックインストール
│   └── rotate_logs.sh          # ログローテーション
│
├── src/                        # プロジェクト本体
├── tests/                      # テストコード
└── docs/                       # ドキュメント
```

## 7つのエージェントロール

| ロール | 責務 | 監視ディレクトリ |
|--------|------|------------------|
| Feature Builder | 新機能の実装 | `tasks/features/` |
| Bug Fixer | バグ修正 | `tasks/bugs/` |
| Refactorer | リファクタリング | `tasks/refactor/` |
| Test Writer | テスト追加 | `tasks/tests/` |
| Reviewer | コードレビュー・問題発見 | Git log |
| Optimizer | パフォーマンス改善 | `tasks/optimize/` |
| Documentarian | ドキュメント整備 | `tasks/docs/`, `logs/` |

## タスクの作成

### タスクファイルの形式

```markdown
# タスク: ユーザー認証の実装

## ステータス: open
## 優先度: high

## 概要
JWT ベースのユーザー認証エンドポイントを実装する

## 完了条件
- [ ] POST /auth/login が正しいトークンを返す
- [ ] tests/test_auth.py の全テストが通る
- [ ] 既存テストが壊れていない

## 参考
- 設計ドキュメント: docs/auth_design.md
```

### タスクの配置

```bash
# 機能実装タスク
vi tasks/features/001_user_auth.md

# バグ修正タスク
vi tasks/bugs/042_sql_injection.md

# リファクタリングタスク
vi tasks/refactor/005_dry_auth.md
```

## エージェントの起動方法

### 方法1: チーム一括起動（推奨）

```bash
# scripts/launch_team.sh を編集してチーム構成を定義
vi scripts/launch_team.sh

# TEAM配列を編集:
# TEAM=(
#   "feature_builder:A"
#   "feature_builder:B"
#   "bug_fixer:A"
#   "refactorer:A"
#   # ...
# )

# チームを起動
./scripts/launch_team.sh
```

### 方法2: 個別起動

```bash
# 単一エージェントを起動
./scripts/launch_agent.sh feature_builder A

# 特定のタスクを指定
./scripts/launch_agent.sh bug_fixer B tasks/bugs/042_sql_injection.md
```

## tmux操作

| 操作 | キー |
|------|------|
| ペイン移動 | `Ctrl+B` → `矢印キー` |
| スクロールモード | `Ctrl+B` → `[` |
| ペインズーム | `Ctrl+B` → `Z` |
| セッション切断 | `Ctrl+B` → `D` |

```bash
# 再接続
tmux attach -t agents

# セッション停止
./scripts/launch_team.sh --kill
```

## 進捗の確認

### 1. status.sh で確認

```bash
./scripts/status.sh
```

出力例:
```
=== プロジェクト稼働状況 ===
  時刻: 2026-02-11 10:05:00

■ タスク状況
  features      未着手:   5  完了:   3
  bugs          未着手:   2  完了:   7
  ...

■ ロック中のタスク
  🔒 features__002_api_endpoint
     担当: feature-builder-A  開始: 2026-02-11T10:01:00+09:00
     経過時間: 15分

■ エージェントログ
  feature-builder-A: feat-002 実装中、認証ミドルウェア追加完了
  bug-fixer-A: bug-023 修正完了
```

### 2. ログファイルで確認

```bash
# 進捗ログ（自動更新）
cat logs/feature-builder-A/progress.md

# セッションログ（全入出力）
tail -f logs/feature-builder-A/session_20260211_100000.log
```

### 3. 全体サマリー

```bash
# Documentarian が更新する全体サマリー
cat logs/_summary.md
```

## カスタマイズ

### 1. プロンプトのカスタマイズ

```bash
# 基本プロンプト（全エージェント共通）
vi .claude/base_prompt.md

# ロール別プロンプト
vi .claude/roles/feature_builder.md
vi .claude/roles/bug_fixer.md
# ...

# プロトコル
vi .claude/protocols/lock_protocol.md
vi .claude/protocols/commit_protocol.md
vi .claude/protocols/test_protocol.md
```

### 2. チーム構成のカスタマイズ

```bash
# scripts/launch_team.sh の TEAM 配列を編集
vi scripts/launch_team.sh
```

### 3. テストスクリプトの実装

```bash
# プロジェクトのテストコマンドを実装
vi ci/run_fast.sh
vi ci/run_full.sh
```

## メンテナンス

### ログのローテーション

```bash
# デフォルト: 各ロール10セッション保持
./scripts/rotate_logs.sh

# 保持件数を指定
./scripts/rotate_logs.sh 5

# 削除対象の確認のみ
./scripts/rotate_logs.sh --dry-run
```

### 長時間ロックの解除

```bash
# 1時間以上ロックされているタスクを確認
./scripts/status.sh

# 手動でロックを解除
rm locks/features__001_user_auth.lock
git add locks/features__001_user_auth.lock
git commit -m "Manual unlock: エージェント停止のため"
git push
```

## トラブルシューティング

### Q1. エージェントが起動しない

A. 以下を確認：
- Claude Code がインストールされているか: `claude --version`
- プロンプトファイルが存在するか: `ls .claude/`
- 実行権限があるか: `chmod +x scripts/*.sh`

### Q2. テストが失敗する

A. 以下を確認：
- `ci/run_fast.sh` と `ci/run_full.sh` が実装されているか
- テストコマンドが正しいか
- 必要な依存関係がインストールされているか

### Q3. ロックが解放されない

A. 以下を確認：
- エージェントが正常に終了したか
- `./scripts/status.sh` で長時間ロックを確認
- 必要に応じて手動でロックを削除

### Q4. 進捗ログが更新されない

A. 以下を確認：
- `./scripts/setup_hooks.sh` が実行されているか
- `.git/hooks/post-commit` が存在するか
- コミットメッセージが `[role-id]` の形式になっているか

## 参考文献

- [Building a C Compiler](https://www.anthropic.com/engineering/building-c-compiler) - Anthropic Engineering Blog
- 設計思想の詳細: [docs/reference.md](docs/reference.md)
- Claude Code公式ドキュメント: https://code.claude.com/docs/

## ライセンス

MIT License

## 貢献

プルリクエストを歓迎します。大きな変更の場合は、まずIssueで議論してください。

## サポート

- GitHub Issues: https://github.com/your-repo/ai-driven-development-poc/issues
- ドキュメント: docs/
