> **実行ロジック移行済み**: ブランチ整理の実行ロジックは `.claude/hooks/session-start-branch-cleanup.sh` に移行済み。本文書は設計仕様リファレンスとして維持する。

# セッション開始時のブランチ整理ルール

## 適用対象

- L1 マネージャーセッション（`.claude/skills/l1-manager/SKILL.md`）
- トリアージマネージャーセッション（`.claude/skills/triage-manager/SKILL.md`）

**L2 サブエージェント（worker / evaluator）は対象外**。L2 は L1 からサブエージェントとして起動されるため、ブランチ管理は L1 の責務である。

## SDK 環境でのスキップ

SDK 環境（Claude Code Web）では、セッション開始時のブランチ整理フロー全体をスキップする。

### 判定条件

現在のブランチ名が `claude/` プレフィックスで始まる場合、SDK 環境と判定する。

### スキップ理由

SDK 環境では以下の理由からブランチ整理が不要かつ有害である。

1. **ブランチの所有権**: SDK 環境では各セッションに `claude/<session-type>-<id>` ブランチが割り当てられ、そのブランチ上で作業を継続する。main への切り替えは不要。
2. **並列セッションへの影響**: ブランチ整理で main に切り替えると、同じリポジトリで動作する他の SDK セッションの作業に影響しうる。
3. **PR 操作の制約**: SDK 環境では `claude/` プレフィックスブランチでないと `gh` コマンドによる PR 作成等の操作ができない。`initiative/<施策名>` ブランチを別途作成せず、セッションに割り当てられた `claude/` ブランチをそのまま施策ブランチとして使用する。

### hook での実装

ステップ 1（現在ブランチ確認）の直後に判定を行い、`claude/` プレフィックスの場合は `action: "sdk_skip"` を返して早期リターンする。ステップ 2〜6 は実行されない。

## 実行タイミング

**新規セッション開始時（startup）のみ**実行する。セッション再開時（resume）は実行しない。

- 並列セッション環境（SDK 等）では、resume 時にブランチ整理が走ると他セッションの作業ブランチが意図せず main に戻る問題があるため、resume は対象外とする。
- `.claude/settings.json` の matcher 設定: `"startup"`

## フロー

以下の 6 ステップを順に実行する。

### ステップ 1: 現在ブランチの確認

```bash
git branch --show-current
```

- 結果が `main` の場合 → ステップ 5 の `git pull` のみ実行し、ステップ 6 へ進む
- 結果が `claude/` プレフィックスの場合 → SDK スキップ（上記「SDK 環境でのスキップ」参照）
- 結果が `main` 以外の場合 → ステップ 2 へ進む

### ステップ 2: 未プッシュコミットの確認

```bash
git log origin/<現在のブランチ>..HEAD --oneline
```

- 出力がある場合（未プッシュコミットあり）→ ステップ 3 へ
- 出力がない場合（全コミットプッシュ済み）→ ステップ 4 へ
- リモートブランチが存在しない場合 → ステップ 3 へ（初回プッシュが必要）

### ステップ 3: 未プッシュコミットのプッシュ

```bash
git push -u origin <現在のブランチ>
```

- プッシュ成功 → ステップ 4 へ
- プッシュ失敗 → エラーを報告し、ユーザーに判断を仰ぐ（セッションを停止する）

### ステップ 4: PR の確認・作成

```bash
gh pr list --head <現在のブランチ> --base main --state open
```

- PR が存在する場合 → PR の URL を確認メッセージとして表示し、ステップ 5 へ
- PR が存在しない場合 → 以下で PR を作成する

```bash
gh pr create --base main --head <現在のブランチ> --title "<ブランチ名の要約>" --body "前セッションからの継続作業。セッション開始時のブランチ整理で自動作成。"
```

- PR 作成成功 → 作成した PR の URL を表示し、ステップ 5 へ
- PR 作成失敗 → エラーを報告し、ユーザーに判断を仰ぐ

### ステップ 5: main ブランチへの切り替え・最新化

```bash
git checkout main && git pull
```

- 成功 → ステップ 6 へ
- 失敗 → エラーを報告し、ユーザーに判断を仰ぐ

### ステップ 6: 通常のセッションフローへ

ブランチ整理が完了した旨をユーザーに報告し、スキル定義の通常の作業フロー（入力解釈・テンプレートコピー等）に進む。

## 注意事項

- **マージは行わない**: PR の作成までが本フローの責務。マージはユーザーの判断に委ねる。
- **PR 作成済みならブロックしない**: 前セッションの PR がマージ待ちでも、main に切り替えて新しい作業を開始してよい。
- **同じ施策の続きでも整理する**: 前セッションと同じブランチにいても、一旦 main に戻してから再度ブランチに入る（壁打ちフェーズで施策を確認した後にブランチ切り替えを行う）。

---
**作成日**: 2026-03-07
**関連施策**: `sessions/initiatives/session-branch-cleanup-flow/`
**追記**: 2026-03-24（SDK 環境でのブランチ制約 → SDK 環境でのスキップに統合）
