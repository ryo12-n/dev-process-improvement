# タスクリスト: backlog-to-issue-scheduled

## 凡例
- ステータス: ⬜ 未着手 / 🔄 進行中 / ✅ 完了 / ⛔ ブロック
- 優先度: 🔴 高 / 🟡 中 / 🔵 低

## 実施ワーカー（l2-worker）タスク

| ID | タスク | 完了条件 | 優先度 | ステータス |
|----|--------|---------|--------|-----------|
| T-001 | `select-backlog-candidate.sh` を改修: (1) 優先度フィルタを撤廃し全優先度対応に変更、(2) ソート順を優先度ASC（低=1,中=2,高=3）→起票日ASCに変更、(3) 出力に `id`（BL-XXX）を追加 | スクリプトが全優先度の候補を低→中→高順で選定し、`id` を `$GITHUB_OUTPUT` に出力する | 🔴 | ✅ |
| T-002 | `backlog-to-issue.yml` に `schedule` トリガーを追加: `cron: '0 */6 * * *'`。schedule 用ジョブ `scheduled-create` を新設し、(1) checkout、(2) `select-backlog-candidate.sh` 実行、(3) 候補なし時の正常終了、(4) 重複チェック（`gh issue list` で `[BL-XXX]` タイトル検索）、(5) Issue 作成、(6) `initiative-wallbash.yml` ディスパッチ のステップを実装。既存の `create-issue` ジョブ（workflow_dispatch 用）はそのまま維持する | schedule トリガーで候補選定→重複チェック→Issue 作成→wallbash ディスパッチが動作する。GHA セキュリティルール（`${{ }}` を `env:` 経由で渡す）に準拠している | 🔴 | ✅ |
| T-003 | `backlog-candidate-propose.yml` を `git rm` で削除する | ファイルが削除されコミットに含まれている | 🔴 | ✅ |
| T-004 | `backlog-auto-execute.yml` を `git rm` で削除する | ファイルが削除されコミットに含まれている | 🔴 | ✅ |
| T-005 | 作業中に発見した知見を `04_work_report.md` の「作業中の知見」セクションに記録する。「ルール化候補」と「参考情報」に分類し、各テーブルに最低1行記載する（該当なしの場合は「なし — 理由: ○○」と記載） | 「ルール化候補」「参考情報」の両テーブルに最低1行の記載がある | 🔴 | ✅ |
| T-006 | `07_issues.md` の未転記課題を確認し、施策をまたぐ課題を `プロセス改善_課題管理.csv` へ転記する。転記時は `issues/_template.md` をコピーして `issues/entries/ISS-XXX.md` を作成し、課題詳細を記載する。CSV の `詳細ファイル` 列に `issues/entries/ISS-XXX.md` のパスを記入する | 全課題に `[転記済 ISS-XXX]` または「転記不要」の判断が付いている | 🔴 | ✅ |

## 評価ワーカー（l2-evaluator）タスク

| ID | タスク | 完了条件 | 優先度 | ステータス |
|----|--------|---------|--------|-----------|
| T-101 | 評価計画に基づき成果物を評価する。成功基準5項目への達成度、GHA セキュリティ準拠、既存 workflow_dispatch の回帰有無を確認する | 評価レポートに全項目の評価結果とエビデンスが記載されている | 🔴 | ⬜ |
| T-102 | 評価中に発見した知見を `06_eval_report.md` の「評価中の知見」セクションに記録する。「ルール化候補」と「参考情報」に分類し、各テーブルに最低1行記載する（該当なしの場合は「なし — 理由: ○○」と記載） | 「ルール化候補」「参考情報」の両テーブルに最低1行の記載がある | 🔴 | ⬜ |
| T-103 | 評価中に新たに発見した課題を `07_issues.md` へ起票し、施策をまたぐ課題を `プロセス改善_課題管理.csv` へ転記する。転記時は `issues/_template.md` をコピーして `issues/entries/ISS-XXX.md` を作成し、課題詳細を記載する。CSV の `詳細ファイル` 列に `issues/entries/ISS-XXX.md` のパスを記入する | 全課題に `[転記済 ISS-XXX]` または「転記不要」の判断が付いている | 🔴 | ⬜ |

---
**作成者**: L1
**最終更新**: 2026-03-15
