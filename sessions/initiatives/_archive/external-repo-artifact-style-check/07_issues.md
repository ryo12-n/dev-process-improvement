# 課題メモ（施策内バッファ）: external-repo-artifact-style-check

> **このファイルの役割**: 施策実施中に気づいた課題・知見・リスクの一時メモ置き場。
> 施策完了（または定期レビュー）時に `issues/entries/ISS-XXX.md` を作成する（CSV は自動生成）

## 転記ルール

- 施策スコープ外・他の施策でも再発しうる課題 → **CSV に転記**
- 当施策内だけで完結する軽微な修正メモ → 転記不要（クローズ扱い）
- 転記済みの項目には `[転記済 ISS-XXX]` を末尾に追記する

---

## 未転記メモ

### [2026-03-19] GHA execute ワークフローが .claude/ ディレクトリへの書き込みを許可しない
- **種別**: 技術的制約
- **内容**: `claude-code-action@v1` は `.claude/` ディレクトリへの書き込みを "sensitive file" として拒否する。GHA execute ワークフローから `.claude/skills/` 配下のファイルを変更することができない。このため、T-001（l2-worker.md の変更）は GHA コンテキストでは実行不可。
- **対応方針（案）**: `.claude/` 配下のファイル変更を伴う施策は CLI モードで実行する。または `initiative-execute.yml` ワークフローの許可設定を調整する（ただし claude-code-action の制約がハードコードされている場合は変更不可）。
- **関連タスク**: T-001
