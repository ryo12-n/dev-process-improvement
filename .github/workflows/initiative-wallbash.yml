name: Initiative Wallbash

on:
  workflow_dispatch:
    inputs:
      issue_number:
        description: 'Issue number to process'
        required: true
        type: number

jobs:
  wallbash:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
      issues: write
      id-token: write
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Extract initiative info from issue
        id: info
        env:
          GH_TOKEN: ${{ github.token }}
          ISSUE_NUMBER: ${{ inputs.issue_number }}
        run: |
          # Issue タイトルと本文を取得
          issue_json=$(gh issue view "$ISSUE_NUMBER" --json title,body)
          title=$(echo "$issue_json" | jq -r '.title')
          body=$(echo "$issue_json" | jq -r '.body')
          echo "title=$title" >> "$GITHUB_OUTPUT"

          # Issue 本文を一時ファイルに保存（Claude のプロンプトで参照）
          echo "$body" > /tmp/issue_body.txt

      - name: Run Claude Code
        id: claude
        uses: anthropics/claude-code-action@v1
        with:
          allowed_bots: "github-actions[bot]"
          anthropic_api_key: ${{ secrets.ANTHROPIC_API_KEY }}
          prompt: |
            `.claude/skills/gha-wallbash/SKILL.md` を Read ツールで読み取り、その指示に従って作業してください。
            このタスクは GitHub Actions の自動化ワークフローから実行されています。

            ## 動的コンテキスト
            Issue #${{ inputs.issue_number }}: ${{ steps.info.outputs.title }}
            Issue 本文は `/tmp/issue_body.txt` に保存されています。
          claude_args: "--max-turns 30 --model claude-sonnet-4-6"
          show_full_output: true
          settings: |
            {
              "permissions": {
                "allow": [
                  "Bash(git *)",
                  "Bash(ls *)",
                  "Read",
                  "Write",
                  "Edit",
                  "Glob",
                  "Grep"
                ]
              }
            }

      - name: Extract initiative name
        id: extract
        env:
          EXECUTION_FILE: ${{ runner.temp }}/claude-execution-output.json
        run: |
          # ブランチ名から施策名を取得
          branch=$(git branch --show-current)
          if [ "$branch" != "main" ]; then
            initiative_name=$(echo "$branch" | sed 's|^initiative/||')
          else
            echo "::error::Claude did not create an initiative branch"
            exit 1
          fi
          echo "initiative_name=$initiative_name" >> "$GITHUB_OUTPUT"

          # execution_file からサマリーを抽出
          if [ -f "$EXECUTION_FILE" ]; then
            summary=$(jq -r '
              [.[] | select(.type == "assistant") | .message.content[]? | select(.type == "text") | .text] | last // empty
            ' "$EXECUTION_FILE" 2>/dev/null)
          fi

          if [ -z "$summary" ]; then
            summary="（壁打ちサマリーの自動取得に失敗しました。施策ディレクトリの 00_proposal.md を参照してください。）"
          fi

          {
            echo "summary<<SUMMARY_EOF"
            echo "$summary"
            echo "SUMMARY_EOF"
          } >> "$GITHUB_OUTPUT"

      - name: Post initiative metadata
        env:
          GH_TOKEN: ${{ github.token }}
          ISSUE_NUMBER: ${{ inputs.issue_number }}
          INITIATIVE_NAME: ${{ steps.extract.outputs.initiative_name }}
        run: |
          gh issue comment "$ISSUE_NUMBER" --body "<!-- initiative-metadata
          initiative_name: ${INITIATIVE_NAME}
          initiative_dir: sessions/initiatives/${INITIATIVE_NAME}/
          branch: initiative/${INITIATIVE_NAME}
          -->
          📋 **施策メタデータ**
          | 項目 | 値 |
          |------|-----|
          | 施策名 | \`${INITIATIVE_NAME}\` |
          | ディレクトリ | \`sessions/initiatives/${INITIATIVE_NAME}/\` |
          | ブランチ | \`initiative/${INITIATIVE_NAME}\` |"

      - name: Create PR
        id: pr
        env:
          GH_TOKEN: ${{ github.token }}
          ISSUE_NUMBER: ${{ inputs.issue_number }}
          ISSUE_TITLE: ${{ steps.info.outputs.title }}
        run: |
          # 現在のブランチ名を取得
          branch=$(git branch --show-current)
          if [ "$branch" = "main" ]; then
            echo "::warning::Claude did not create a branch. Skipping PR creation."
            echo "pr_url=" >> "$GITHUB_OUTPUT"
            exit 0
          fi

          echo "branch=$branch" >> "$GITHUB_OUTPUT"

          # PR を作成（権限エラー時はスキップ）
          pr_url=$(gh pr create \
            --base main \
            --head "$branch" \
            --title "initiative: ${ISSUE_TITLE}" \
            --body "## 施策: ${ISSUE_TITLE}

          Issue: #${ISSUE_NUMBER}

          壁打ちフェーズで作成された施策提案。
          \`@ai-task /approve\` コメントで実行フェーズに進みます。" 2>&1) || echo "::warning::PR creation failed. Check repository settings for Actions permissions."

          echo "pr_url=$pr_url" >> "$GITHUB_OUTPUT"

      - name: Post wallbash summary to issue
        env:
          GH_TOKEN: ${{ github.token }}
          ISSUE_NUMBER: ${{ inputs.issue_number }}
          SUMMARY: ${{ steps.extract.outputs.summary }}
          INITIATIVE_NAME: ${{ steps.extract.outputs.initiative_name }}
          BRANCH: ${{ steps.pr.outputs.branch }}
          PR_URL: ${{ steps.pr.outputs.pr_url }}
          REPO: ${{ github.repository }}
        run: |
          # 成果物リンクを構築
          artifacts="### 成果物
          - [\`00_proposal.md\`](https://github.com/${REPO}/blob/${BRANCH}/sessions/initiatives/${INITIATIVE_NAME}/00_proposal.md)"

          # PR リンク
          pr_link=""
          if [ -n "$PR_URL" ]; then
            pr_link="
          ### Pull Request
          - ${PR_URL}"
          fi

          gh issue comment "$ISSUE_NUMBER" --body "## 壁打ちサマリー

          ${SUMMARY}

          ${artifacts}
          ${pr_link}

          ---
          **次のステップ**: 内容を確認し、\`@ai-task /approve\` コメントで実行フェーズに進んでください。フィードバックがある場合は \`@ai-task /approve <フィードバック内容>\` の形式でコメントしてください。"

      - name: Update labels
        env:
          GH_TOKEN: ${{ github.token }}
          ISSUE_NUMBER: ${{ inputs.issue_number }}
        run: |
          # initiative + initiative:wallbashing を付与
          gh issue edit "$ISSUE_NUMBER" \
            --add-label "initiative,initiative:wallbashing" 2>/dev/null || true
